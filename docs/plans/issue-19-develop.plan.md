# Issue #19 Develop Kickoff Plan

## Objective
CI // Fix PR implementation workflow UID home-path failure

## Source Issue
- URL: https://github.com/CursedFactory/cursed-fab/issues/19
- Stage: Develop
- Generated by workflow: `issue-stage-develop.yml`

## Problem Statement
# Overview
- Goal: make `pr-stage-implement.yml` actually execute `opencode` when running under mapped host UID.
- Context: workflow runs with `-u $(id -u):$(id -g)` so file writes work, but OpenCode fails with `EACCES: permission denied, mkdir "/.local"` because HOME is unset for that UID.
- Expected outcome: workflow sets a writable HOME path before invoking OpenCode and can proceed with implementation runs.

# Gate Conditions
- [ ] PR implementation workflow sets writable HOME for mapped UID
- [ ] Workflow no longer fails with `/.local` permission error
- [ ] Dispatch/issue_comment run demonstrates at least one successful OpenCode execution step

# Verification
- [ ] Local container sanity check with mapped UID and HOME override
- [ ] Workflow run evidence link showing successful implementation stage execution

## Thread Highlights
- No thread comments yet.

## Kickoff Tasks
- Confirm implementation scope from issue body and thread highlights.
- Add/adjust implementation commits on the linked PR branch.
- Update this plan as the implementation details become concrete.

## Verification Targets
- Workflow and automation changes are validated via `gh run` evidence.
- Documentation reflects any new stage triggers or commands.
