# Issue Template

# Overview
- Goal:
- Context:
- Expected outcome:

# Gate Conditions
- [ ] Devcontainer / docker compose (and required containers) build cleanly
- [ ] Required CLI(s) run successfully in-container (example: `opencode --version`)
- [ ] GitHub Action workflow runs without container/runtime errors
- [ ] Workflow posts a useful response back to issue/PR context when applicable

# Design
- Base runtime (devcontainer/service):
- Execution prompt/input source:
- Output destination (comment, artifact, log):

# Implementation Notes
- Workflow file(s) to change:
- Container/config file(s) to change:
- Secrets/env requirements:

# Verification
- Local checks run:
- CI evidence/run links:
- Risks or follow-ups:
